<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ article.title }} - My Blog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        h3 {
            color: #333;
        }

        img {
            max-width: 100%;
            height: auto;
            margin-top: 10px;
        }

        p {
            margin-top: 10px;
            color: #555;
        }

        small {
            color: #777;
        }

        a.back-link {
            display: block;
            margin-top: 20px;
            color: #007bff;
            text-decoration: none;
        }

        a.back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<a href="/articles" class="back-link">TAKE ME BACK</a>

<h3>Title: {{ article.title }}</h3>

<img src="{{ article.picture }}" alt="{{ article.title }}" />

<p>Description: {{ article.description }}</p>

<p><small>Created at: {{ article.createdAt.format('Y-m-d H:i:s') }}</small></p>

</body>
</html>
